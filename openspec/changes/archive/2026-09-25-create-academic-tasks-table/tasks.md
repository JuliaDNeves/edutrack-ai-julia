# Tasks

## 1. Schema Definition

- [x] 1.1 Criar o arquivo de definição de tabela `tables/academic_tasks.xs` contendo o esquema da tabela `academic_tasks` (campos `title`, `description`, `due_date`, `status`, `subject_id`, `user_id`, `created_at` e seus índices) e verificar sua integridade sintática XanoScript.
