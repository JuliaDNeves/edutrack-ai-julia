# Tasks

## 1. Table Schema Definition

- [x] 1.1 Create `tables/subjects.xs` with fields `id` (auto-increment primary key), `name` (text), `teacher` (text), `hours` (integer), and `user_id` (foreign key to Xano authentication table `user`/`users`)
